# Global styles

**Category:** Styling (site-wide)  
**Purpose:** CSS variables, resets, and `#root` layout used across all pages.

Page-specific styles belong next to the page, e.g. `webservices/pages/home/HomePage.css`.
