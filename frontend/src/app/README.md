# App shell

**Category:** Application wiring  
**Purpose:** Router and root component only — no business UI, no API calls.

| File | Purpose |
|------|---------|
| `App.tsx` | `BrowserRouter` + `Routes` |
| `routes.tsx` | Maps URL paths to page components |

Add new routes in `routes.tsx`; implement pages under `webservices/pages/`.
