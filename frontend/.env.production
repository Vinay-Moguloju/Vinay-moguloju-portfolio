# GitHub Pages: leave VITE_API_BASE_URL unset so the UI uses portfolioContent.ts (no 404s).
# When you host the Java API, set a full URL before build, e.g.:
# VITE_API_BASE_URL=https://your-api.example.com/api
