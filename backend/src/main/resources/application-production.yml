server:
  port: ${PORT:8080}

spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate

portfolio:
  cors:
    allowed-origins: ${PORTFOLIO_CORS_ALLOWED_ORIGINS:https://vinay-moguloju.github.io}
